{
  "name": "raid",
  "version": "2.0.0",
  "description": "Centralised state container",
  "main": "lib/",
  "scripts": {
    "clean": "rm -rf lib _book",
    "prebuild": "npm run clean",
    "build": "babel src --out-dir lib",
    "watch": "babel --watch src --out-dir lib",
    "prestart": "npm run build",
    "start": "node examples",
    "pretest": "npm run build",
    "test": "tape spec/*.js",
    "test-only": "tape spec/*.js",
    "cover": "istanbul cover --report lcovonly --print detail spec/*.js",
    "travis": "npm run cover && cat coverage/lcov.info | coveralls",
    "prepublish": "npm run build",
    "predocs": "npm run clean && gitbook install",
    "docs": "gitbook build",
    "docs:watch": "gitbook serve",
    "docs:deploy": "npm run docs && cd _book && git init && git commit --allow-empty -m 'update book' && git checkout -b gh-pages && git add . && git commit -am 'update book' && git push git@github.com:mattstyles/raid gh-pages --force"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "babel": {
    "presets": [
      "urban"
    ],
    "plugins": [
      [
        "transform-react-require",
        {
          "identifier": "Inferno",
          "moduleName": "inferno"
        }
      ],
      "inferno"
    ]
  },
  "standard": {
    "parser": "babel-eslint"
  },
  "keywords": [
    "raid",
    "stream",
    "scan",
    "state",
    "signal",
    "component"
  ],
  "author": "Matt Styles",
  "repository": {
    "type": "git",
    "url": "https://github.com/mattstyles/raid"
  },
  "bugs": "https://github.com/mattstyles/raid/issues",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-eslint": "^7.0.0",
    "babel-plugin-inferno": "^0.7.7",
    "babel-plugin-transform-react-require": "^1.0.1",
    "babel-preset-urban": "^0.5.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "budo": "^9.2.1",
    "coveralls": "^2.11.14",
    "gh-pages": "^0.9.0",
    "immutable": "^3.8.1",
    "inferno": "^0.7.27",
    "inferno-component": "^0.7.27",
    "inferno-dom": "^0.7.27",
    "inquirer": "^1.2.1",
    "istanbul": "^0.4.5",
    "minimist": "^1.2.0",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.4",
    "standard": "^8.3.0",
    "tape": "^4.6.2"
  },
  "dependencies": {
    "eventemitter3": "^2.0.2",
    "most": "^1.0.3"
  }
}
